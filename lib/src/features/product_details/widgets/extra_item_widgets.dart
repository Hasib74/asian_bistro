import 'package:flutter/material.dart';

class ExtraItemWidgets extends StatelessWidget {
  const ExtraItemWidgets({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
